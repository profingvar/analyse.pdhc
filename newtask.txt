analyse.pdhc — BUILT + VERIFIED + COMMITTED (0f47896). 38 tests pass, single
alembic head 0001_initial, analysis-phase gate + gateway/monitor service-key.

NEXT = deploy + cutover (operator-coordinated, HIGH BLAST). See
docs/cutover_runbook.md. Blocked on operator inputs: SSO client registration
for analyse.pdhc, 3 service keys, CDR2-6 URLs, reverse-proxy vhost
analyse.pdhc.se + DNS/TLS. Then: deploy → #540 gateway repoint → #541 CDR
identity flip → #542 verify → #543 delete group half from cd-assist → #547
rebrand + www cards → #544 cleanup.
