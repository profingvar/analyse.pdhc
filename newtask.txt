# analyse.pdhc — current focus

Reconstruction Phases 1–4 + AN-12 (the coordinator↔node wire) are BUILT.
459 tests, gate clean. HEAD is local-only — none of it is deployed.
Live at analyse.pdhc.se is still 0.2.0-reform (deployed 2026-09-03).

## Next
- #685 deploy the reconstruction. Read that ticket before starting: the
  cdr2–5 federation wiring (#541) and the dashboard→analyse repoint (#540)
  are DEPLOYED-ONLY, not in git, and a wholesale deploy drops them.
- #686 gap G6 — the federated stack has still never run against real CDRs.
  AN-12 removed the blocker (there is a wire now); the rest is deployment.
- #688 Phase 3 usability walkthrough (acceptance outstanding).
- #687 gap G9 — keyed_hash has no identifier source. A decision, not code.

## Carried gap from AN-12
A node cannot derive its cohort from `spec.cohort.include`:
`run_spec()` never reads the inclusion criteria, and the CDR has no
criterion-search endpoint. `ConfiguredCohortSource` (operator pins the
cohort per node) is what a deployment gets today. Ticketed separately.

## Operator live-smoke never confirmed (from the 2026-09-03 reform)
Landing "choose patient", per-patient dashboard from CDR2-6, spärr badge +
admin spärr-log, names via ips /clinics/<guid>/patients. SSO-gated, needs a
human with a real professional token. Folded into #685's acceptance.
